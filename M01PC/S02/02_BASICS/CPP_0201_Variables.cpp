#include <iostream>
using namespace std;

int main() {
    /*
    Variable/Constant
    Memory space in order to store some data
    - Label (Name)
    - Type
    - Value
    */
    int address = 10;
    int total;

    int age;            // Declaring a variable
    age = 30;           // Initialize a variable

    bool isSingle;      // Declaring a variable
    isSingle = true;    // Initialize a variable

    return 0;
}
